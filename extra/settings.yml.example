--- 
# HTTPS settings
#:ssl_certificate: /var/lib/puppet/ssl/certs/FQDN.pem
#:ssl_private_key: /var/lib/puppet/ssl/private_keys/FQDN.pem
#:ssl_ca_file: /var/lib/puppet/ssl/certs/ca.pem

# Enable TFTP management
#:tftp: true
#:tftproot: /home/paul/git/foreman/tftpboot

# Enable DNS management
#:dns: true
#:dns_vendor: isc
#:dns_key: /etc/smart-proxy/Kdynamic-update.+157+29820.key

# Enable DHCP management
#:dhcp: true
# The vendor can be either isc or native_ms
#:dhcp_vendor: ISC
# The dhcp_server is only used by the native_ms implementation
#:dhcp_server: 172.29.216.54
# Enable caching of the results to reduce the load on the underlying DHCP server and lower the latency
#:dhcp_cache_ttl: 300
#:dhcp_cache_enabled: true
# Settings for Ubuntu ISC
#:dhcp_config: /etc/dhcp3/dhcpd.conf
#:dhcp_leases: /var/lib/dhcp3/dhcpd.leases
# Settings for Redhat ISC
#:dhcp_config: etc/dhcpd.conf
#:dhcp_leases: etc/dhcpd.leases

# enable PuppetCA management
#:puppetca: true

# enable Puppet management
#:puppet: true

# Where our proxy log files are stored
# filename or STDOUT
# Unix setting
#:log_file: /var/log/smart-proxy/smart-proxy.log
# Windows setting
#:log_file: c:\tmp\smart-proxy.log
# valid options are 
# Logger::WARN, Logger::DEBUG, Logger::Error, Logger::Fatal, Logger:INFO, LOGGER::UNKNOWN
#:log_level: Logger::DEBUG

# Run the daemon as another account(Unix only)
#:user: smart-proxy
#:group: smart-proxy
